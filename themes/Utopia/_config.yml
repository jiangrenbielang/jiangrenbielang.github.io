# Utopia 主题 — 站点/个人数据请放在 source/_data/*.yml
# 模板读取 site.data.site / site.data.menu / site.data.musics 等

# 功能开关
comments:
  enable: false   # 同时需要在 source/_data/comments.yml 配置 appId/appKey

# 灰色模式：none / index | all
gray: none

# 日志页超过此字数折叠
log_text_num: 1000

# 主题版本
version: 1.0.0
